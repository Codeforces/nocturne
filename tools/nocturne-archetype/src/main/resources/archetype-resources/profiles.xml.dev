<?xml version="1.0" encoding="UTF8"?>
<profilesXml>
    <profiles>
        <profile>
            <id>development-settings</id>
            <properties>
                <!-- Directory to the root of the project. -->
                <development.home>${basedir}/${artifactId}</development.home>

                <!-- True iff you want to use the debug mode. -->
                <nocturne.debug>true</nocturne.debug>

                <!-- Path to write log file. -->
                <logging.path>${development.home}/${artifactId}.log</logging.path>
            </properties>
        </profile>
    </profiles>
    <activeProfiles>
        <activeProfile>development-settings</activeProfile>
    </activeProfiles>
</profilesXml>
