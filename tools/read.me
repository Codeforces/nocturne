Run:
    "mvn install" in nocturne-archetype

And after run:

    mvn archetype:create -DarchetypeGroupId=org.nocturne.archetypes \
        -DarchetypeArtifactId=nocturne-archetype -DarchetypeVersion=1.0-SNAPSHOT \
        -DgroupId=?  -DartifactId=?
